<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Education Details Form</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow p-4">
      <h3 class="text-center mb-4">🎓 Education Details</h3>

      <form action="/employee/education_process" method="post">
        <!-- Degree -->
        <div class="mb-3">
          <label for="degree" class="form-label">Degree</label>
          <input type="text" class="form-control" id="degree" placeholder="e.g. Bachelor of Science" name="degree" required>
        </div>

        <!-- Institution -->
        <div class="mb-3">
          <label for="institution" class="form-label">Institution</label>
          <input type="text" class="form-control" id="institution" placeholder="e.g. Mumbai University" name="institution" required>
        </div>

        <!-- Field of Study -->
        <div class="mb-3">
          <label for="field" class="form-label">Field of Study:</label>
          <input type="text" class="form-control" id="field" placeholder="e.g. Computer Science" name="field_of_study" required>
        </div>

        <!-- Start Year -->
        <div class="mb-3">
          <label for="startYear" class="form-label">Start Year:</label>
          <input type="number" class="form-control" id="startYear" placeholder="enter year"name="start_year">
        </div>
        <div class="mb-3">
          <label for="score" class="form-label">end year:</label>
          <input type="number" class="form-control" id="score" placeholder="enter year" required  name="end_year">
        </div>

         
        <!-- Percentage / CGPA -->
        <div class="mb-3">
          <label for="score" class="form-label">Percentage / CGPA:</label>
          <input type="text" class="form-control" id="score" step="0.01" placeholder="e.g. 85% or 9.0 CGPA" name="percentage">
        </div>


        <div class="text-center">
          <button type="submit" class="btn btn-primary px-4">Submit</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
